# COPY THIS FILE to .env for local development.

# apps/web's dev server. iOS Simulator can use localhost directly; a
# physical device or Android emulator needs the host machine's LAN IP
# (Android emulator's host alias is 10.0.2.2).
EXPO_PUBLIC_API_URL="http://localhost:3000"
